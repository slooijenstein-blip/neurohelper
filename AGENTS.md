TanStack Start uses file-based routing in `src/routes/`. Do not rewrite published git history unless the user asks.

After any user-visible fix, run `npm run deploy:pages` so GitHub Pages matches local.
